# Core machine learning / symbolic-music stack
torch>=2.1
music21>=8.0
numpy>=1.24

# Used by the optional preprocessing notebook / loss plotting helpers
tqdm>=4.64

# Optional: MIDI -> audio playback (see README)
# pyfluidsynth>=1.3.2